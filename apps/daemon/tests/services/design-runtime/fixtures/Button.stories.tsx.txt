import type { Meta, StoryObj } from '@storybook/react';
import { Button as ProductionButton } from './Button';

const meta = {
  component: ProductionButton,
  title: 'Fixture/Button',
  args: { variant: 'secondary', disabled: true },
  argTypes: {
    variant: { options: ['primary', 'secondary', 'danger'], control: { type: 'select' }, description: 'Visual variant' },
  },
  tags: ['autodocs'],
} satisfies Meta<typeof ProductionButton>;
export default meta;

type Story = StoryObj<typeof meta>;
export const Primary: Story = { name: 'Primary example', args: { variant: 'primary', disabled: false } };
export const Danger: Story = { args: { variant: 'danger' } };
