# Design Loom 界面设计记录

2026-09-07，在已安装的 Design Loom 中使用真实 Codex 先生成设计稿，再据此实施概览改进。设计项目为 **Design Loom 概览设计**，项目 ID 为 `a92bfceb-8bfe-47ad-9e62-0378f53061d8`，对话 ID 为 `9e9e8e38-e777-4437-925a-c37210dac663`。

## 设计来源与审阅

首次设计运行 `ac030252-6180-4824-9781-e4a76d2652e2` 已完成，产出可交互 `index.html` 与 `design-notes.md`。设计稿保留在上述应用项目中；审阅快照的 HTML SHA-256 为 `e5f144ddde3eb31539e786d189856bd7fb084e5bd46da0f6852cf549e3b61825`。

已在原生应用中查看实际排版，并操作「已迁移并锁定」「有可编辑基线」「全新空项目」三种审阅状态与只读版本详情。稿中的 Stripe `1.0.2`、45 个 tokens、6 份来源、零代码组件和页面来自验收问题描述；它们是设计稿样例，不是此新设计项目的实时数据。设计任务采用 Explore 模式，完成状态不表示 Strict 认证。

## 采用的设计

- 沿用应用白色面板、浅灰背景、细边框与少量绿色强调，保留概览、组件、预览和更多导航。
- 优先显示设计系统名称、精确版本和当前使用状态，让已经迁移的成果可见。
- 基础样式、代码组件与页面同层级展示，来源文件单独列出。tokens 和源文件不计作代码组件。
- 锁定状态引导进入现有版本管理；可编辑状态说明编辑基线并给出继续入口；全新项目保持简单的开始路径。
- 加载失败或版本证据不匹配时显示无法确认，不以零值代替未知数据。

实现使用现有 HTTP 版本接口读取并核对真实包；原型中的状态切换器、模拟创建草稿和模拟详情交互不进入产品。冻结版本的编辑仍遵循现有解除依赖、保留编辑基线和显式发布流程。

## 设置文案补充

同一设计项目的后续真实运行 `ac6b86e6-fb93-4eca-ad65-a35735351d84` 已完成，将以下最终文案加入 `design-notes.md`，未修改产品源码：

- 独立应用使用 Design Loom 品牌，第三方 OpenDesign Cloud 保留真实名称。产品侧栏继续保留「实验室」功能语义。
- API 模式说明为「支持读取、创建和修改项目文件，需要可用的 OpenCode（本机安装或应用随附）」。
- 未检测到 OpenCode 时说明 API 模式暂不可用，并提供安装和重新扫描入口。

实施复用现有 OpenCode 检测、安装文档及重新扫描能力。设计稿建议的前往 CLI 入口会按现有产品行为适配：查看依赖提示或重新扫描不应自动改变用户已保存的执行模式。API 连接测试与执行依赖检测各自保留真实含义。

## 预览属性的未声明值

系统安装版原生验收发现：完整 JSON 可将 `outline`、`lg` 原样传给组件，但这两个值不在验收 Button 的声明中，下拉框因此错误回显首项 `default`。合法的 `primary`、`default` 会正确同步；导入实际主题的 `ProductButton.tsx` 能正常渲染、修改文案、切换禁用状态和重置。该问题属于控件回显，不据此判定 CSS Module 失效。

同一设计项目的真实 Codex 运行 `cb55022a-54fe-45a7-8490-4a10bf7b2c36` 已成功完成，新增 `preview-enum.html` 与 `preview-enum-notes.md`，保留原两份设计文件。最终 HTML SHA-256 为 `1a40f5e04fa6f8c61bd971c59b6c97550c9ffe7d14c3d2586cbe7b1891074545`，设计说明 SHA-256 为 `c6da641addc5bdf9d4f1da295386fe84e09a88eefb91ecc6b64c83f8756c1b25`。

原生审阅已操作未声明值、改选合法值、逐项恢复、无效 JSON 与恢复路径。最终稿中从 `primary/default` 手动输入 `{` 后，控件与预览仍保留上次有效值；点击「恢复上次有效 JSON」后重新可编辑。采用的设计是字段内显示实际值和「未声明」标识，配一行中性说明；错误 JSON 保留草稿并暂停会覆盖它的字段编辑，提供显式恢复入口。

正式实施继续使用真实属性元数据及现有重置规则：保留预览样例值或省略字段以使用源码默认值，不照抄原型中的 `default` 字面量。JSON 原值、类型与额外属性保持不变；原型的状态切换按钮和内联示意组件不进入产品。此交互设计不授予未知值类型认证，也不引入主题加载改造。

## 历史任务错误归属（设计完成，待原生审阅）

系统安装版显示了真实的归属错误：旧 Guided 逻辑任务在一次有界修复后仍被阻止，后来独立任务已经接受交付，刷新却将旧失败显示在最新成功回复下方。旧失败必须保留；后续成功不抹去历史报告，也不能将前驱物理运行结束当作整个逻辑任务成功。

同一设计项目的真实 Codex 运行 `37778736-7bfa-4b73-bdcf-cc0b165eb392` 于 2026-09-07 完成，新增 `run-status.html` 与 `run-status-notes.md`，原四份文件哈希未变。最终 HTML SHA-256 为 `8af218d3040560c4c5be28063b3b04d286e3535a443e4ed454477f9f5400552d`，说明 SHA-256 为 `54532a9c65382d2ede1477e12122696f80707b02541b9cf42e78bb72f1017bb8`。物理与逻辑终态均为 `succeeded`；Explore 报告为 advisory、0 错误、792 条提示、`strictReady: false`。独立入口检查仍返回 `deliverableValid: false / entry_not_touched`，因为原 `index.html` 未修改；新增的两份请求文件已逐一核验，不能将该入口检查说成通过。

设计稿提供最新成功、最新失败、运行与重连三种审阅样例，复用聊天、局部失败卡及验证报告。旧失败与修复明细留在所属逻辑轮次，晚到事件不改变当前轮次；物理运行、验证结论与 Strict 就绪分别表达。稿中的场景切换、模拟同步和简化任务标识不代表真实 API 或实时业务数据。

此前 OpenCode 运行 `3857602f-2f5f-4923-a56c-9353ffc92901` 正常退出但没有交付文件；续写 `7b89d0f3-504a-417b-88ed-ec53269e5408` 遇到响应格式错误，在一次有界自动重试后由验收负责人通过原生界面停止。它们分别记录为未交付与取消，不记为设计完成。

设计文件已经静态审阅。原生工具在打开最终稿时报告 Mac 再次锁屏，因此三种场景的原生操作和产品实施仍待解锁后继续。已补齐错误归属的失败复现测试，生产实现保持未改；测试不能替代设计稿原生审阅。

实现及安装验收状态见[验收报告](design-loom-acceptance.md)，日常操作见[使用指南](design-loom.md)。
